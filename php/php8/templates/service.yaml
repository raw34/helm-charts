apiVersion: v1
kind: Service
metadata:
  name: {{ include "php8.fullname" . }}
  labels:
    {{- include "php8.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "php8.selectorLabels" . | nindent 4 }}
