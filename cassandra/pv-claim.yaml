apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-cassandra-0
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  volumeName: cassandra-pv-volume
